<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="DataWiz.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls">

    <Grid Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top bar -->
        <StackPanel Orientation="Horizontal" Spacing="8" Grid.Row="0">
            <Button Content="Load CSV" Click="LoadButton_Click"/>
            <Button Content="Export CSV" Click="ExportButton_Click"/>
            <TextBlock Text="{Binding StatusText}"
                       VerticalAlignment="Center" Margin="12,0,0,0"/>
        </StackPanel>

        <!-- Data grid -->
        <controls:DataGrid x:Name="DataGridControl"
                            Grid.Row="1"
                            Margin="0,12,0,0"
                            ItemsSource="{Binding Rows}"
                            AutoGenerateColumns="False"
                            IsReadOnly="True"/>
    </Grid>
</Page>